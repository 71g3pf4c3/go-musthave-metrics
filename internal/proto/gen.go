// Package proto contains the generated gRPC code for the metrics service.
//
// Regenerate the *.pb.go files (protoc and plugins are provided by the
// nix dev shell) with:
//
//go:generate protoc --proto_path=. --go_out=paths=source_relative:. --go-grpc_out=paths=source_relative:. metrics.proto
package proto
